packages:
  # generate_surrogate_key, used in stg_confido__product_shipping_configs - the one
  # source table with no id column of its own.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
