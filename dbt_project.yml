name: 'confido_case'
version: '1.0.0'
config-version: 2

# matches the top-level key in profiles.yml
profile: 'default'

clean-targets:
  - "target"
  - "dbt_packages"

models:
  confido_case:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
